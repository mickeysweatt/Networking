Must do
-------
o Parser the raw request and response using provided frameworks

o HTTP Client: take a HTTPRequest Object, connect to the destination server, get
             the page, cache it, return to requester.
             
o Handle the caching
---------------------------------------------------------------------------------
Optional (if we have time and want to do something cool)
o Performance (Different concurrent models)
	o threads with a shared cache in main mem, which will be stored cached on disk)
	o Process pool 
		o which each store a copy of the cache
		o condition variable for when they have a job
		o stored with a map of process to a list of servers that they have open TCP connections to (pipelining and all)